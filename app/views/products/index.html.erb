<h2>Products:</h2>

<% @products.each do |product| %>
  <%= link_to product_path(product.id) do %>
    <h3><%= product.name %></h3>
  <% end %>
  <p><%= product.description %></p>
  <p><%= "$. #{product.price}" %></p>
<% end %>